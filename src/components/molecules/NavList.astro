---
import NavLink from "../atoms/NavLink.astro";
import { navItems } from "../../utils/constants/navItems.js";

interface Props {
  variant?: "desktop" | "sidebar" | "footer";
}

const { variant = "desktop" } = Astro.props;
---

<ul class={variant}>
  {
    navItems.map((item) => (
      <li>
        <NavLink href={item.href} label={item.label} variant={variant} />
      </li>
    ))
  }
</ul>

<style>
  /* desktop (Header, wide viewport) */
  .desktop {
    width: 100%;
    height: 100%;
    margin: 0px auto;
    padding: 0px;
    display: flex;
    justify-content: space-around;
    align-items: center;
    z-index: 2;
  }

  .desktop li {
    list-style-type: none;
    display: flex;
    font-size: 2rem;
    height: 100%;
    width: 100%;
    justify-content: center;
    align-items: center;
    transition: 0.3s;
    background-color: white;
  }

  .desktop li:hover {
    cursor: pointer;
    background-color: #262626;
  }

  /* sidebar (Header, hamburger menu) */
  .sidebar {
    padding-left: 0px;
  }

  .sidebar li {
    list-style-type: none;
    line-height: 3rem;
    opacity: 1;
  }

  /* footer */
  .footer {
    list-style-type: none;
    display: flex;
    gap: 2em;
  }

  @media (max-width: 768px) {
    .footer {
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: 0;
      margin: 0;
      width: 100%;
    }
    .footer li {
      width: 50%;
      text-align: center;
    }
  }
</style>
